name: movies_bloc_example
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^5.0.0
  flutter_bloc: ^5.0.0
  freezed_annotation: ^0.11.0
  http: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  freezed: ^0.11.2
  json_serializable: ^3.3.0

flutter:
  uses-material-design: true
  